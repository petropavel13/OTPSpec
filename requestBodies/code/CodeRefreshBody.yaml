title: CodeRefreshBody
description: "Тело запроса для обновления кода подтверждения"
type: object
properties:
  confirmationId:
    $ref: "../../schemas/ConfirmationId.yaml"
required:
  - confirmationId
